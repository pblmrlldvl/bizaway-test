{
  "name": "bizaway-test",
  "version": "0.0.1",
  "author": "PabloMurillo",
  "private": true,
  "main": "dist/index.js",
  "engines": {
    "node": ">=16.15.0"
  },
  "scripts": {
    "start": "node dist/index.js",
    "start:dev": "NODE_ENV=development nodemon --watch src -e ts --exec 'ts-node src/index.ts'",
    "build": "npm-run-all clean tsc",
    "tsc": "tsc",
    "clean": "rm -rf dist",
    "test": "jest"
  },
  "directories": {
    "dist": "dist"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^22.10.7",
    "jest": "^29.7.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.7.1",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
